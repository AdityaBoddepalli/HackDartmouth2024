import {
   Popover,
   PopoverTrigger,
   PopoverContent,
   PopoverHeader,
   PopoverBody,
   PopoverFooter,
   PopoverArrow,
   PopoverCloseButton,
   PopoverAnchor,
   Button
} from '@chakra-ui/react'

import AutoModeIcon from '@mui/icons-material/AutoMode';

export default function Badge({ url }: { url: string }) {
   

   return (
      <Button>Trigger</Button>
   );
}